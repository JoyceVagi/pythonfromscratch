"""we can also iterate using the index of elements"""
list = ["happy", "day", "coding"]
"""idea is then to find the lenght of the index"""
for index in range(len(list)):
    print(list[index])
    """iterates over the sequence within the range of the list"""